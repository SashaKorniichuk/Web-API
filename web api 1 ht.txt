WEB API

Дописати два методи HttpPut для оновлення інформації, про конкретний фільм та HttpDelete, для видалення фільма з бази.

В ході розробки дописати методи, які будуть необхідні, якщо така потреба виникне (наприклад щось типу GetById).

Зробити міграцію та додати до таблиці поле Image
На головній сторінці показати перелік фільмів. 

Продемонструвати роботу всіх методів на UI. Використати HTML/CSS/JS  (Bootstrap)


Інтерфейс довільний - але гарний :) 
